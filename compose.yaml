services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        API_BASE_URL: ${API_BASE_URL:-http://localhost:8002/api}
    container_name: flutter-task-manager-web
    ports:
      - "${APP_PORT:-8080}:80"
